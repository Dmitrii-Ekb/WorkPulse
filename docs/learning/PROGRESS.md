# Прогресс обучения WorkPulse

Обновлено: **2026-07-18**

## Текущая точка

Minimal API для задач работает с SQLite через Entity Framework Core. Данные сохраняются после перезапуска приложения.

Строка подключения хранится в `appsettings.json`, читается через `GetConnectionString("WorkPulseDatabase")` и проверяется при запуске приложения.

Модель базы данных `WorkTask` вынесена из `Program.cs` в отдельный файл `WorkTask.cs`.

Для `POST /tasks` создан входной DTO `CreateWorkTaskDto`. Клиент передаёт только `Title`, а сервер самостоятельно создаёт сущность `WorkTask`, устанавливает `IsCompleted = false` и позволяет базе назначить `Id`.

Проверены сценарии:

- корректное создание задачи через DTO;
- пустое и состоящее из пробелов название возвращает `400 Bad Request`;
- отсутствующее поле `title` приводит к проверке пустой строки;
- переданные клиентом `id` и `isCompleted` не попадают в DTO и не управляют создаваемой сущностью;
- добавление DTO не изменяет схему базы и не требует новой миграции;
- проект успешно собирается.

Следующая тема: **асинхронная работа с Entity Framework Core**.

Первый небольшой шаг — перевести только `GET /tasks` с `ToList()` на `ToListAsync()` и разобраться, зачем Web API не должен блокировать поток во время ожидания базы данных.

## Текущее состояние проекта

### Модель базы данных

Сущность находится в отдельном файле `WorkTask.cs`:

```csharp
namespace WorkPulse;

public class WorkTask
{
    public int Id { get; set; }
    public string Title { get; set; } = "";
    public bool IsCompleted { get; set; }
}
```

`WorkTask` описывает данные задачи, которые сохраняются в таблице `Tasks`.

Класс остаётся `public`, потому что он используется публичным свойством `DbSet<WorkTask>` в контексте базы.

### Входной DTO

В отдельном файле `CreateWorkTaskDto.cs` находится контракт запроса на создание задачи:

```csharp
namespace WorkPulse;

public class CreateWorkTaskDto
{
    public string Title { get; set; } = "";
}
```

`CreateWorkTaskDto` не является сущностью базы данных и не входит в `DbSet`.

Его задача — описать только те данные, которые разрешено передавать клиенту при создании задачи.

Текущая цепочка создания:

```text
JSON
  ↓
CreateWorkTaskDto request
  ↓ валидация и явное преобразование
WorkTask newTask
  ↓
EF Core
  ↓
SQLite
```

### База данных и EF Core

Подключены пакеты:

- `Microsoft.EntityFrameworkCore.Sqlite` версии `9.0.17`;
- `Microsoft.EntityFrameworkCore.Design` версии `9.0.17`.

Установлен глобальный инструмент:

- `dotnet-ef` версии `9.0.17`.

Создан `WorkPulseDbContext`:

```csharp
public class WorkPulseDbContext : DbContext
{
    public WorkPulseDbContext(
        DbContextOptions<WorkPulseDbContext> options)
        : base(options)
    {
    }

    public DbSet<WorkTask> Tasks { get; set; } = null!;
}
```

### Конфигурация подключения

В `appsettings.json` создана секция:

```json
"ConnectionStrings": {
  "WorkPulseDatabase": "Data Source=workpulse.db"
}
```

В `Program.cs` строка подключения читается и проверяется:

```csharp
var connectionString =
    builder.Configuration.GetConnectionString("WorkPulseDatabase");

if (string.IsNullOrWhiteSpace(connectionString))
{
    throw new InvalidOperationException(
        "Не найдена строка подключения " +
        "'ConnectionStrings:WorkPulseDatabase'. " +
        "Проверь файл appsettings.json.");
}

builder.Services.AddDbContext<WorkPulseDbContext>(options =>
    options.UseSqlite(connectionString));
```

Проверены два сценария:

- при правильном ключе приложение запускается и работает с существующей базой;
- при неправильном или отсутствующем ключе приложение останавливается при запуске с понятной ошибкой.

### Миграции

Создана первая миграция:

```text
InitialCreate
```

В методе `Up` она создаёт таблицу `Tasks`:

- `Id` — `INTEGER`, первичный ключ, `AUTOINCREMENT`;
- `Title` — `TEXT NOT NULL`;
- `IsCompleted` — `INTEGER NOT NULL`.

В методе `Down` таблица `Tasks` удаляется.

Миграция применена командой:

```text
dotnet ef database update
```

После применения появились:

- файл SQLite `workpulse.db`;
- таблица `Tasks`;
- служебная таблица `__EFMigrationsHistory`;
- служебная таблица блокировки миграций.

Файл базы и служебные SQLite-файлы добавлены в `.gitignore`:

```gitignore
workpulse.db
workpulse.db-shm
workpulse.db-wal
```

Папка `Migrations` сохраняется в Git, потому что она описывает воспроизводимую историю изменений схемы базы.

Добавление `CreateWorkTaskDto` не потребовало новой миграции, потому что DTO не является частью модели EF Core и не меняет таблицу `Tasks`.

## Реализованные маршруты

- `GET /hello`;
- `GET /tasks`;
- `GET /tasks/first`;
- `GET /tasks/{id}`;
- `POST /tasks`;
- `PATCH /tasks/{id}/complete`;
- `DELETE /tasks/{id}`.

### Работа маршрутов с базой

`GET /tasks`:

```csharp
db.Tasks.ToList()
```

Синхронно читает все задачи из таблицы.

Этот маршрут станет первым примером перехода на асинхронный EF Core.

`GET /tasks/first`:

```csharp
db.Tasks
    .OrderBy(task => task.Id)
    .FirstOrDefault()
```

Возвращает задачу с наименьшим `Id` или `404`, если таблица пуста. Старая ошибка с `tasks[0]` устранена.

`GET /tasks/{id}`:

```csharp
db.Tasks.Find(id)
```

Ищет задачу по первичному ключу.

`POST /tasks`:

```csharp
app.MapPost("/tasks",
    (CreateWorkTaskDto request, WorkPulseDbContext db) =>
{
    if (string.IsNullOrWhiteSpace(request.Title))
    {
        return Results.BadRequest(
            "Название задачи обязательно");
    }

    var newTask = new WorkTask
    {
        Title = request.Title,
        IsCompleted = false
    };

    db.Tasks.Add(newTask);
    db.SaveChanges();

    return Results.Created(
        $"/tasks/{newTask.Id}",
        newTask);
});
```

Клиентский JSON десериализуется в `CreateWorkTaskDto`.

После валидации сервер явно создаёт `WorkTask`. Поля `Id` и `IsCompleted` не берутся из клиентского запроса:

- `Id` назначает SQLite;
- `IsCompleted` устанавливает сервер;
- `Title` переносится из DTO после проверки.

`Add` помечает сущность как подготовленную к добавлению, а `SaveChanges` выполняет запись в SQLite.

`PATCH /tasks/{id}/complete`:

- задача находится через `Find`;
- `IsCompleted` меняется на `true`;
- `SaveChanges` сохраняет изменение.

`DELETE /tasks/{id}`:

- задача находится через `Find`;
- `Remove` помечает её на удаление;
- `SaveChanges` выполняет удаление в базе.

Временный маршрут `/db-test` удалён после проверки провайдера.

Старый `List<WorkTask>` полностью удалён из `Program.cs`.

## Проверенное поведение

- пустое название отклоняется с `400 Bad Request`;
- название из пробелов отклоняется с `400 Bad Request`;
- отсутствующая задача возвращает `404 Not Found`;
- создание возвращает `201 Created`;
- удаление возвращает `204 No Content`;
- `Id` назначается SQLite автоматически;
- `IsCompleted` при создании устанавливается сервером в `false`;
- клиент не может задать `Id` или начальное значение `IsCompleted` через DTO;
- созданная задача доступна через `GET`;
- изменение `IsCompleted` сохраняется в базе;
- удалённая задача исчезает из базы;
- данные остаются после перезапуска приложения;
- пустой `/tasks/first` возвращает `404`, а не вызывает исключение;
- неверный ключ строки подключения останавливает приложение с понятной ошибкой;
- проект успешно собирается после вынесения модели и добавления DTO.

## Уже изучено на практике

### C#

- переменные и вывод типов через `var`;
- массивы и их фиксированный размер;
- `List<T>`, `Add`, `Remove`, `Count`;
- анонимные объекты;
- создание собственного класса;
- объект как экземпляр класса;
- свойства с `get` и `set`;
- изменение свойств объекта;
- базовое понимание ссылочных типов;
- разница между изменением объекта и переназначением переменной;
- `null`;
- отличие `null`, пустой строки и строки из пробелов;
- `string.IsNullOrWhiteSpace`;
- лямбда-выражения;
- LINQ: `FirstOrDefault`, `Max`, `OrderBy`, `ToList`;
- ранний `return`;
- интерполяция строк;
- разные классы остаются разными типами, даже если у них совпадают свойства;
- создание объекта одного типа на основе данных объекта другого типа;
- организация классов по отдельным файлам без изменения пространства имён.

### JSON и преобразование данных

- JSON-массив и JSON-объект;
- сериализация C#-объектов в JSON;
- десериализация тела запроса в C#-объект;
- десериализация запроса создания в `CreateWorkTaskDto`;
- неизвестное JSON-свойство по умолчанию не создаёт новое свойство C#;
- дополнительные `id` и `isCompleted` игнорируются при десериализации в DTO, где таких свойств нет;
- отсутствующее `title` оставляет `Title` пустым благодаря значению по умолчанию;
- необходимость пустой строки между HTTP-заголовками и телом запроса;
- явное преобразование DTO в сущность базы.

### ASP.NET Core и HTTP

- Minimal API;
- смысл `MapGet`, `MapPost`, `MapPatch`, `MapDelete`;
- маршрут как сопоставление URL и обработчика;
- параметр маршрута `{id}`;
- GET, POST, PATCH и DELETE;
- различие запроса и ответа;
- тело запроса;
- коды `200`, `201`, `204`, `400`, `404` и базовое понимание `500`;
- `Results.Ok`;
- `Results.Created`;
- `Results.NoContent`;
- `Results.BadRequest`;
- `Results.NotFound`;
- роль `app.Run()` и почему маршруты регистрируются до него;
- DTO как отдельный контракт входного запроса;
- разделение входного API-контракта и сущности базы;
- ограничение набора полей, которыми может управлять клиент;
- базовое понимание проблемы overposting.

### Конфигурация ASP.NET Core

- назначение файла `appsettings.json`;
- секции и ключи JSON-конфигурации;
- секция `ConnectionStrings`;
- чтение строки подключения через `GetConnectionString`;
- получение конфигурации через `builder.Configuration`;
- ранняя проверка обязательной настройки;
- отделение настройки подключения от кода регистрации EF Core.

### Базы данных

- различие базы данных и СУБД;
- реляционная таблица;
- строка, столбец и тип данных;
- схема базы как описание структуры;
- первичный ключ;
- автоинкремент;
- ограничения таблицы на базовом уровне;
- SQLite как файловая СУБД;
- отличие структуры таблицы от данных внутри неё;
- смысл `SELECT`, `INSERT`, `UPDATE` и `DELETE` на концептуальном уровне;
- сохранение данных вне памяти процесса;
- понимание, что изменение DTO не меняет схему базы данных.

### Entity Framework Core

- EF Core не является базой данных;
- SQLite-провайдер связывает EF Core с SQLite;
- `DbContext` как рабочая точка доступа к базе;
- `DbSet<WorkTask>` как представление набора сущностей;
- регистрация контекста через `AddDbContext`;
- получение контекста в параметрах endpoint-а;
- `ToList` для чтения;
- `Find` для поиска по первичному ключу;
- `Add`, изменение свойства и `Remove` как подготовка изменений;
- `SaveChanges` как фактическая отправка изменений в базу;
- базовая идея отслеживания изменений;
- миграция как переход схемы базы из одного состояния в другое;
- назначение методов `Up` и `Down`;
- назначение `__EFMigrationsHistory`;
- миграция нужна при изменении модели EF Core, а не при добавлении обычного DTO.

### Git

- базовая роль `git status`;
- `git add`;
- `git commit`;
- различие между локальным `commit` и отправкой через `push`;
- аналогия между `git add` и подготовкой изменений EF Core полезна, но это разные механизмы;
- локальный файл базы не должен попадать в репозиторий;
- миграции должны сохраняться в Git;
- проверка сборки перед фиксацией законченного шага.

## Проведённые эксперименты и найденные ошибки

1. Использование `tasks[id - 1]` перепутало ID сущности с индексом массива.
2. Запрос несуществующего индекса вызвал `IndexOutOfRangeException`.
3. Изменение ID на `20` показало, почему поиск должен идти по свойству `Id`.
4. `FirstOrDefault` вернул объект или `null`.
5. Попытка изменить свойство анонимного объекта показала его неизменяемость.
6. Массив не позволил вызвать `Add`, после чего использован `List<WorkTask>`.
7. POST без пустой строки после заголовка был ошибочно разобран HTTP-клиентом.
8. Поле `name` вместо `title` не заполнило `Title`.
9. Валидация после добавления была бы слишком поздней.
10. Клиентский ID позволял создавать некорректные значения при хранении в списке.
11. `FirstOrDefault` при дубликатах вернул первую найденную задачу.
12. `Max` на пустом списке не смог вычислить значение.
13. Изменение объекта через локальную переменную изменило тот же объект в списке.
14. После перезапуска сервера данные из списка исчезли.
15. Пакет `Microsoft.EntityFrameworkCore.Design` сам по себе не устанавливает команду `dotnet ef`.
16. Для CLI отдельно установлен `dotnet-ef` той же версии `9.0.17`.
17. Первая миграция показала сгенерированные `CreateTable`, `PrimaryKey`, `Up` и `Down`.
18. После `database update` SQLite создал таблицу точно по миграции.
19. После перехода на SQLite данные сохранились после перезапуска.
20. Без `SaveChanges` подготовленное добавление, изменение или удаление не записывается в базу.
21. Старая реализация `/tasks/first` могла падать; новая возвращает безопасный `404`.
22. Неверное имя строки подключения привело к ранней и понятной ошибке запуска.
23. Перенос `WorkTask` в отдельный файл не изменил модель EF Core и не потребовал миграции.
24. После замены параметра POST с `WorkTask` на `CreateWorkTaskDto` компилятор не разрешил передать DTO в `DbSet<WorkTask>`.
25. Явное создание `new WorkTask` решило несовпадение типов и отделило входной контракт от сущности.
26. Отправка `id` и `isCompleted` в JSON показала, что DTO принимает только объявленное свойство `Title`.
27. POST с пустым объектом `{}` создал DTO, у которого `Title` остался пустой строкой, после чего сработала валидация.
28. Добавление DTO не потребовало миграции, потому что таблица `Tasks` не изменилась.

## Введено, но пока не считать уверенно изученным

Эти конструкции уже встретились в рабочем коде, но требуют повторения и отдельных небольших упражнений:

- конструкторы;
- наследование;
- вызов `base(...)`;
- обобщения на примере `DbContextOptions<T>` и `DbSet<T>`;
- dependency injection;
- жизненный цикл `DbContext`;
- отслеживание состояний сущностей глубже базовой идеи;
- генерируемый EF Core SQL;
- ограничения таблиц кроме первичного ключа;
- исключения и `InvalidOperationException`;
- nullable reference types глубже текущего использования;
- DTO в более сложных операциях;
- отдельные DTO запроса и ответа.

## Что пока не считать изученным

- написание SQL вручную;
- внешние ключи на практике;
- связи таблиц;
- `JOIN`;
- индексы;
- транзакции;
- уникальные ограничения;
- асинхронные методы EF Core;
- `async` и `await`;
- окружения ASP.NET Core;
- значимые типы в сравнении со ссылочными;
- методы класса как отдельная тема;
- инкапсуляция;
- полиморфизм;
- интерфейсы;
- middleware;
- контроллеры;
- автоматическое тестирование;
- авторизация;
- Docker и развёртывание;
- архитектурные паттерны.

## Уровень понимания и стиль обучения

Исходный уровень:

- основы синтаксиса C# знакомы;
- ООП почти с нуля;
- Web API и ASP.NET Core с нуля;
- Git на начальном уровне;
- архитектура почти с нуля.

Предпочтительный подход:

- практический проект вместо абстрактного курса;
- сначала изменение и эксперимент, потом объяснение;
- небольшие шаги;
- объяснение простыми словами и аналогиями;
- ученик регулярно формулирует происходящее своими словами;
- не считать тему освоенной после простого копирования кода;
- при новом синтаксисе останавливаться и разбирать только необходимую часть;
- не перескакивать к сложной архитектуре раньше времени;
- не заканчивать сессию без решения ученика;
- после завершённого этапа наставник предлагает имя коммита, а ученик самостоятельно выполняет `commit` и `push`;
- подробно останавливаться на Git-командах только при новой теме Git или при возникновении ошибки.

## Следующий конкретный шаг

Перевести только маршрут `GET /tasks` с синхронного чтения:

```csharp
db.Tasks.ToList()
```

на асинхронное:

```csharp
await db.Tasks.ToListAsync()
```

На этом шаге нужно разобраться:

1. зачем обработчик endpoint-а становится `async`;
2. что делает `await`;
3. чем `ToListAsync` отличается от `ToList`;
4. почему ожидание базы данных не должно без необходимости блокировать поток Web API;
5. почему асинхронность не означает автоматическое создание нового потока;
6. как проверить, что HTTP-поведение маршрута не изменилось.

Не переводить весь CRUD за один раз. После понимания `GET /tasks` переносить остальные операции по одному маршруту.
