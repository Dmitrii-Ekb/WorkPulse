# Карта обучения WorkPulse

Обновлено: **2026-07-20**

## Цель маршрута

Через последовательное развитие WorkPulse научиться самостоятельно проектировать, писать, отлаживать, тестировать и развёртывать .NET-сервисы.

Конечная цель — уверенный уровень Middle C#/.NET-разработчика и проект, который можно содержательно обсуждать на собеседовании.

Маршрут адаптивный и не привязан к жёстким срокам. Следующая тема начинается после практического понимания предыдущей.

## Текущая веха

WorkPulse имеет:

- работающий асинхронный Minimal API;
- постоянное хранение задач в SQLite;
- Entity Framework Core;
- входной DTO;
- две применённые миграции;
- обязательный заголовок;
- необязательное описание;
- ручную практику базового SQL.

Работает вертикальная цепочка:

```text
HTTP-запрос
    ↓
DTO и валидация
    ↓
асинхронный endpoint
    ↓
сущность WorkTask
    ↓
DbContext и Change Tracker
    ↓
LINQ или изменение состояния сущности
    ↓
SQL-команда с параметрами
    ↓
SQLite
    ↓
C#-объекты и HTTP-ответ
```

Завершены ближайшие улучшения:

- строка подключения вынесена в конфигурацию;
- обязательная настройка проверяется при запуске;
- `WorkTask` вынесен из `Program.cs`;
- создан `CreateWorkTaskDto`;
- входной контракт отделён от сущности;
- клиент не управляет `Id` и начальным `IsCompleted`;
- весь CRUD переведён на асинхронные методы;
- найден и прочитан SQL EF Core;
- доказано отложенное выполнение LINQ;
- `SELECT`, `INSERT`, `UPDATE`, `DELETE` сопоставлены с кодом приложения;
- основные SQL-команды написаны вручную;
- создана и применена вторая миграция;
- добавлено nullable-свойство `Description`;
- описание сохраняется и читается через API;
- разобраны `ModelSnapshot` и `__EFMigrationsHistory`.

Текущая контрольная точка:

```text
асинхронный CRUD работает
        ↓
SQL EF Core прочитан
        ↓
базовый SQL написан вручную
        ↓
вторая миграция применена
        ↓
Description работает от JSON до SQLite и обратно
        ↓
обсудить выходной контракт
        ↓
создать первый интеграционный тест
```

## Этап 1. База C# через реальный код

Статус: **в процессе**

Уже применялось:

- переменные, типы и выражения;
- условия;
- массивы и `List<T>`;
- классы, объекты и свойства;
- `null`;
- nullable reference types на базовом уровне;
- строки и валидация;
- лямбды;
- базовый LINQ;
- `Where`, `OrderBy`, `FirstOrDefault`, `ToList`;
- ранний `return`;
- интерполяция строк;
- создание объектов;
- преобразование DTO в сущность;
- организация классов по файлам;
- асинхронные лямбды;
- `Task`, `async`, `await` на практическом уровне;
- отложенное выполнение LINQ.

Ещё предстоит закрепить:

- методы;
- циклы;
- конструкторы;
- ссылочные и значимые типы;
- nullable-типы системно;
- исключения;
- делегаты;
- даты и перечисления;
- обобщения системно;
- управление памятью;
- асинхронность, параллелизм и многопоточность глубже базовой модели.

Ближайшее развитие C#:

- явное преобразование сущности в DTO ответа;
- методы преобразования после появления повторения;
- дата создания и `DateTime`/`DateTimeOffset`;
- перечисление при добавлении приоритета.

Результат этапа: ученик уверенно читает и пишет обычный C#-код и объясняет, что происходит с объектами, данными и выполнением.

## Этап 2. HTTP и ASP.NET Core

Статус: **в процессе**

Уже применялось:

- HTTP-запрос и ответ;
- GET, POST, PATCH, DELETE;
- маршруты и параметры;
- JSON;
- коды статуса;
- Minimal API;
- валидация;
- dependency injection `DbContext`;
- конфигурация через `appsettings.json`;
- DTO входного запроса;
- обязательные и необязательные свойства запроса;
- ограничение полей клиента;
- асинхронные endpoint-ы;
- сохранение HTTP-контракта после рефакторинга.

Текущая тема:

- различие сущности, входного DTO и выходного DTO;
- стабильность публичного JSON-контракта;
- явное формирование ответа.

Ближайшие темы:

- DTO ответа;
- более строгая валидация;
- редактирование задачи;
- выбор между `PUT` и `PATCH`;
- окружения ASP.NET Core;
- Swagger/OpenAPI;
- централизованная обработка ошибок;
- middleware;
- контроллеры только при практической необходимости.

Результат этапа: полноценный API с понятным, стабильным и контролируемым контрактом.

## Этап 3. Базы данных и SQL

Статус: **в процессе**

Уже применялось:

- база данных и СУБД;
- реляционная таблица;
- строка, столбец, тип и схема;
- первичный ключ;
- автоинкремент;
- SQLite;
- постоянное хранение;
- чтение и ручное написание `SELECT`;
- ручное использование `WHERE`;
- ручное использование `ORDER BY`;
- ручные `INSERT`, `UPDATE`, `DELETE`;
- `LIMIT`;
- SQL-параметры;
- опасность изменения и удаления без `WHERE`;
- соответствие CRUD и SQL;
- nullable-колонка;
- понимание, когда изменение требует миграции.

Ещё предстоит:

- агрегатные функции;
- группировка;
- внешние ключи;
- уникальные ограничения;
- `JOIN`;
- индексы;
- транзакции;
- планы выполнения на базовом уровне;
- PostgreSQL или SQL Server позже.

Промежуточный результат: ученик самостоятельно пишет простые SQL-команды и связывает их с поведением EF Core.

Полный результат этапа: ученик самостоятельно пишет основные запросы и понимает влияние SQL на корректность и производительность.

## Этап 4. Entity Framework Core

Статус: **в процессе**

Уже применялось:

- ORM и её роль;
- SQLite-провайдер;
- `DbContext`;
- `DbSet<T>`;
- регистрация контекста;
- строка подключения;
- миграция `InitialCreate`;
- миграция `AddTaskDescription`;
- `Up` и `Down`;
- изменение существующей схемы;
- сохранение старых строк при добавлении nullable-колонки;
- `ModelSnapshot`;
- `__EFMigrationsHistory`;
- различие создания и применения миграции;
- понимание пустой миграции;
- базовый Change Tracker;
- состояния `Added`, `Modified`, `Deleted`;
- `Add`, `Remove`, изменение свойства;
- `SaveChanges` и `SaveChangesAsync`;
- `FindAsync`, `ToListAsync`, `FirstOrDefaultAsync`;
- синхронный и асинхронный CRUD;
- LINQ-запросы к базе;
- отложенное выполнение;
- перевод LINQ в SQL;
- SQL-параметры;
- материализация C#-объектов;
- обновление только изменённого свойства;
- получение созданного ключа через `RETURNING`.

Ближайшие темы:

- тестовая база для интеграционных тестов;
- изменение существующих данных через новый endpoint;
- значения по умолчанию при добавлении обязательной колонки;
- связи сущностей;
- границы транзакций;
- отслеживание и `AsNoTracking` после появления практической причины;
- N+1 после появления связей;
- производительность запросов позже.

Промежуточный результат: ученик понимает полный цикл изменения модели и схемы базы.

Полный результат этапа: ученик уверенно моделирует данные, управляет миграциями, пишет запросы и понимает их стоимость.

## Этап 5. ООП и организация кода

Статус: **в процессе**

Уже выполнено:

- `WorkTask` вынесен из `Program.cs`;
- сохранено единое пространство имён;
- создан `CreateWorkTaskDto`;
- сущность отделена от входного контракта;
- выполнено явное преобразование DTO в сущность;
- начато разделение ответственности типов.

Текущая практическая тема:

- отделение сущности от выходного контракта;
- явное преобразование без преждевременных библиотек;
- понимание ответственности каждого типа.

Ближайшие темы вводятся по практической необходимости:

- методы и ответственность;
- инкапсуляция;
- DTO ответа;
- сервисы;
- endpoint-модули или контроллеры;
- интерфейсы;
- композиция и наследование;
- dependency injection глубже текущего использования;
- SOLID на конкретных проблемах;
- репозиторий только при реальной необходимости, а не автоматически поверх EF Core.

Результат этапа: проект развивается без превращения в один большой файл и без преждевременной архитектурной сложности.

## Этап 6. Качество и тестирование

Статус: **следующий крупный этап**

Ближайшая практическая цель — один интеграционный тест создания задачи.

Темы:

- тестовый проект;
- integration-тесты API;
- тестовая база;
- `WebApplicationFactory`;
- Arrange, Act, Assert;
- проверка HTTP-статуса;
- проверка JSON;
- проверка сохранения данных;
- изоляция тестов;
- unit-тесты позже, когда появится самостоятельная бизнес-логика;
- логирование;
- централизованная обработка ошибок;
- анализаторы и форматирование;
- мок-объекты только там, где они действительно полезны.

Результат: изменения можно вносить уверенно и проверять автоматически.

## Этап 7. Пользователи и безопасность

Статус: **запланирован**

Темы:

- регистрация и вход;
- хеширование паролей;
- authentication и authorization;
- JWT или cookie-аутентификация;
- роли и права;
- защита секретов;
- CORS;
- типовые уязвимости Web API;
- rate limiting;
- SQL-инъекции и роль параметризованных запросов.

Результат: задачи принадлежат конкретным пользователям и защищены правилами доступа.

## Этап 8. Production-навыки

Статус: **запланирован**

Темы:

- конфигурация по окружениям;
- PostgreSQL или SQL Server;
- Docker;
- CI/CD;
- публикация приложения;
- health checks;
- структурированные логи;
- метрики и трассировка;
- резервное копирование;
- миграции при развёртывании;
- message broker после появления подходящего сценария.

Результат: WorkPulse доступен не только локально и поддерживает эксплуатационные требования.

## Этап 9. Подготовка к собеседованиям

Статус: **идёт параллельно**

Формат:

- короткие вопросы после практической темы;
- объяснение решений своими словами;
- чтение кода;
- поиск ошибок;
- обсуждение компромиссов;
- пятничный рефакторинг;
- субботнее мини-собеседование;
- регулярный code review.

Темы собеседований связываются с реальными решениями WorkPulse:

- C#;
- ООП;
- ASP.NET Core;
- HTTP;
- EF Core;
- SQL;
- тестирование;
- архитектура;
- Git;
- Docker и production.

## Ближайшая последовательность

```text
1. Обсудить сущность и DTO ответа
2. При необходимости создать WorkTaskResponseDto
3. Привести WorkPulse.http к повторяемому виду
4. Добавить первый интеграционный тест POST /tasks
5. Добавить редактирование заголовка и описания
6. Добавить дату создания или приоритет новой миграцией
7. Добавить фильтрацию, сортировку и пагинацию
8. Добавить вторую сущность и связь
```

Порядок может корректироваться, но без перескакивания через непонятые основы.
