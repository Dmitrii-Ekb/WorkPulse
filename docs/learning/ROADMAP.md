# Карта обучения WorkPulse

## Цель маршрута

Не просто собрать учебный API, а научиться самостоятельно проектировать, писать, отлаживать и развивать .NET-сервисы.

Конечная цель — уверенная база уровня middle-разработчика и проект, который не стыдно обсуждать на собеседовании.

Маршрут не привязан к жёстким срокам. Переход к следующей теме происходит после практического понимания предыдущей.

## Текущая веха

WorkPulse уже имеет Minimal API и постоянное хранение задач в SQLite через EF Core.

Работает вертикальная цепочка:

```text
HTTP-запрос
    ↓
входной DTO
    ↓
endpoint и валидация
    ↓
сущность WorkTask
    ↓
DbContext
    ↓
EF Core
    ↓
SQLite
    ↓
HTTP-ответ
```

Завершены ближайшие улучшения:

- строка подключения вынесена в `appsettings.json`;
- обязательная настройка проверяется при запуске;
- `WorkTask` вынесен из `Program.cs` в отдельный файл;
- создан `CreateWorkTaskDto`;
- входной контракт создания отделён от сущности базы;
- клиент больше не управляет `Id` и начальным значением `IsCompleted`.

Текущая задача — перевести операции EF Core на асинхронные методы, начиная только с `GET /tasks`.

## Этап 1. База C# через реальный код

Статус: **в процессе**

Уже применялось:

- переменные, типы и выражения;
- условия;
- массивы и `List<T>`;
- классы, объекты и свойства;
- `null`;
- строки и `string.IsNullOrWhiteSpace`;
- лямбды и базовый LINQ;
- ранний `return`;
- интерполяция строк;
- создание объектов;
- преобразование данных между объектами разных типов;
- организация классов по отдельным файлам;
- обобщения встречены в коде EF Core.

Ещё предстоит закрепить:

- методы;
- циклы;
- конструкторы;
- ссылочные и значимые типы;
- nullable-типы;
- исключения;
- делегаты;
- даты и перечисления;
- обобщения;
- `async`/`await`;
- управление памятью на практическом уровне.

Ближайшая тема C# — `Task`, `async` и `await` на реальном запросе к базе данных.

Результат этапа: ученик уверенно читает и пишет обычный C#-код и может объяснить, что происходит с данными и объектами.

## Этап 2. HTTP и ASP.NET Core

Статус: **в процессе**

Уже применялось:

- HTTP-запрос и ответ;
- GET, POST, PATCH и DELETE;
- маршруты и параметры;
- JSON, сериализация и десериализация;
- коды статуса;
- Minimal API;
- базовая валидация входных данных;
- внедрение `DbContext` в параметры endpoint-а на практическом уровне;
- конфигурация через `appsettings.json`;
- чтение строки подключения через `GetConnectionString`;
- ранняя проверка обязательной настройки;
- DTO входного запроса;
- отделение API-контракта от сущности базы;
- ограничение полей, которыми управляет клиент.

Ближайшие темы:

- асинхронные endpoint-ы;
- окружения ASP.NET Core;
- DTO ответа;
- обработка ошибок;
- Swagger/OpenAPI;
- middleware;
- более строгая валидация.

Результат этапа: полноценный Web API с понятным и контролируемым контрактом.

## Этап 3. Базы данных и SQL

Статус: **в процессе**

Уже применялось:

- назначение СУБД;
- таблица, строка, столбец и схема;
- первичный ключ;
- автоинкремент;
- ограничения на базовом уровне;
- SQLite;
- постоянное хранение данных;
- концептуальное соответствие CRUD и `SELECT`, `INSERT`, `UPDATE`, `DELETE`;
- понимание, что DTO не является частью схемы базы;
- понимание, когда изменение требует миграции.

Ещё предстоит:

- написание SQL вручную;
- внешние ключи;
- уникальные ограничения;
- фильтрация и сортировка в SQL;
- `JOIN`;
- индексы;
- транзакции;
- PostgreSQL или SQL Server позже.

Промежуточный результат уже достигнут: данные WorkPulse сохраняются после перезапуска.

Полный результат этапа: данные корректно связаны между собой, а ученик умеет читать и писать основные SQL-запросы.

## Этап 4. Entity Framework Core

Статус: **в процессе**

Уже применялось:

- ORM и её роль;
- SQLite-провайдер;
- `DbContext`;
- `DbSet`;
- регистрация контекста через dependency injection;
- строка подключения из конфигурации;
- миграции;
- `Up` и `Down`;
- история миграций;
- базовое отслеживание изменений;
- `Add`, `Remove`, изменение свойства и `SaveChanges`;
- LINQ-запросы к базе;
- `Find`;
- полный синхронный CRUD;
- понимание, что новая миграция нужна при изменении модели базы, а не при добавлении DTO.

Ближайшие темы:

- асинхронные операции EF Core;
- более явное понимание сгенерированного SQL;
- вторая миграция;
- связи сущностей;
- границы транзакций;
- проблема N+1 позже, когда появятся связи.

Текущий небольшой шаг:

```text
GET /tasks:
ToList() → ToListAsync()
```

Промежуточный результат уже достигнут: API работает с реальной базой через EF Core.

Полный результат этапа: ученик понимает не только вызовы EF Core, но и какой SQL и какие изменения базы примерно стоят за ними.

## Этап 5. ООП и организация кода

Статус: **в процессе**

Уже выполнено:

- `WorkTask` вынесен из `Program.cs` в отдельный файл;
- сохранено единое пространство имён;
- начато разделение ответственности типов;
- создан `CreateWorkTaskDto`;
- сущность базы отделена от входного API-контракта;
- выполнено явное преобразование DTO в сущность.

Ближайшие темы вводятся после появления конкретной неудобной части кода:

- методы и ответственность;
- инкапсуляция;
- DTO ответа;
- интерфейсы;
- композиция и наследование;
- endpoint-модули или контроллеры при необходимости;
- сервисы;
- dependency injection глубже текущего минимального использования;
- репозитории только при реальной необходимости;
- базовые принципы SOLID на конкретных проблемах проекта.

Результат этапа: код можно развивать без превращения проекта в один большой файл и без смешивания внешнего API с внутренней моделью хранения.

## Этап 6. Качество и тестирование

Статус: **запланирован**

Темы:

- unit-тесты;
- integration-тесты API;
- тестовая база;
- Arrange, Act, Assert;
- мок-объекты только там, где они помогают;
- логирование;
- централизованная обработка ошибок;
- валидация;
- рефакторинг;
- анализаторы и форматирование.

Результат: изменения можно вносить уверенно и проверять автоматически.

## Этап 7. Пользователи и безопасность

Статус: **запланирован**

Темы:

- регистрация и вход;
- хеширование паролей;
- authentication и authorization;
- JWT или cookie-аутентификация;
- роли и права;
- защита секретов;
- CORS;
- типовые уязвимости Web API;
- rate limiting.

Результат: задачи принадлежат конкретным пользователям и защищены правилами доступа.

## Этап 8. Production-навыки

Статус: **запланирован**

Темы:

- конфигурация по окружениям;
- Docker;
- CI/CD;
- публикация приложения;
- облачная база данных;
- health checks;
- структурированные логи;
- метрики и трассировка;
- резервное копирование;
- миграции при развёртывании.

Результат: WorkPulse доступен не только на локальном компьютере.

## Этап 9. Темы уровня middle

Статус: **долгосрочная цель**

Темы:

- архитектурные компромиссы;
- границы модулей;
- транзакции и конкурентные изменения;
- optimistic concurrency;
- кэширование;
- фоновые задачи;
- очереди сообщений;
- производительность;
- профилирование;
- надёжность;
- code review;
- декомпозиция требований;
- оценка задач;
- технические решения и их аргументация.

Результат: ученик не только пишет код, но и способен обосновывать решения и видеть риски.
